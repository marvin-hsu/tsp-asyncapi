---
---

No release. `tsp-asyncapi-core` starts at 0.1.0 and `tsp-asyncapi` stays at
0.4.0, both set by hand as part of moving to independent versioning. Neither
has been published yet, so there is nothing to bump.
